# =============================================================
# Contraste Éditions — Variables d'environnement PRODUCTION (Django)
# Copier ce fichier vers .env sur le VPS et remplir les valeurs
# =============================================================

# ----- Next.js / Frontend -----
NEXT_PUBLIC_DJANGO_URL=https://api.contraste.tn

# ----- Django Backend -----
DJANGO_SECRET_KEY=GENEREZ_AVEC_OPENSSL_RAND_BASE64_32
DJANGO_DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,backend,api.contraste.tn
CORS_ALLOWED_ORIGINS=https://contraste.tn,https://www.contraste.tn

# ----- PostgreSQL -----
DB_NAME=contraste
DB_USER=contraste
DB_PASSWORD=GENEREZ_AVEC_OPENSSL_RAND_HEX_24
DB_HOST=postgres
DB_PORT=5432
